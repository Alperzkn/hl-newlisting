# Required
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Optional
HL_API_URL=https://api.hyperliquid.xyz
POLL_INTERVAL_MS=1000
DEX_POLL_INTERVAL_MS=10000
HEARTBEAT_INTERVAL_MIN=60
STATE_FILE_PATH=./data/known-assets.json
TELEGRAM_HEARTBEAT_CHAT_ID=
ENABLE_DESKTOP_SOUND=false
LOG_LEVEL=info

# --- alt.fun / HyperEVM watcher (optional) ---
# When ENABLE_ALTFUN=true, the service ALSO watches a HyperEVM AMM factory for
# PairCreated events and notifies on each new pool. alt.fun graduations migrate
# to a HyperSwap pool, so pointing this at the HyperSwap V2 factory captures
# every graduation plus any other token opening a HyperSwap pool. Narrow with
# ALTFUN_QUOTE_TOKEN to only notify on pools paired against a specific quote
# (e.g., USDC) — leave empty to alert on every pair created.
ENABLE_ALTFUN=false
# alt.fun's pair factory on HyperEVM (Uniswap V3 fork). Verify on the HyperEVM
# explorer before trusting any address you find online.
ALTFUN_FACTORY=0xff7b3e8c00e57ea31477c32a5b52a58eea47b072
# "v3" = Uniswap V3 PoolCreated event (alt.fun's factory style).
# "v2" = Uniswap V2 PairCreated event (HyperSwap V2, etc.).
ALTFUN_FACTORY_KIND=v3
# Optional quote-token filter. LEAVE EMPTY for alt.fun: each alt.fun pool is
# paired against a per-token "tokenized HL perp" that differs by pool, so a
# single-address filter would miss most graduations. The factory itself is the
# filter — only alt.fun graduations create pools here.
ALTFUN_QUOTE_TOKEN=
# OPTIONAL strict-mode filter. When set, ONLY pools where one side is an
# EIP-1167 proxy to this implementation will fire alerts; everything else is
# skipped. Use this if you specifically want alt.fun-only and don't care about
# other launchpads on the same factory (LiquidLaunch, leveraged-HYPE tokens,
# etc.). Leave empty for the broader "every pool on the factory" behavior.
ALTFUN_TOKEN_IMPL=
# Comma-separated list of quote-token addresses on HyperEVM. The watcher uses
# this list to figure out which side of the pool is the new token (whichever
# side ISN'T in the list). Defaults cover alt.fun's spoof-USDC quote and WHYPE
# — extend if you see other quote tokens in alerts.
ALTFUN_QUOTE_ADDRESSES=0xb88339cb7199b77e23db6e890353e22632ba630f,0x5555555555555555555555555555555555555555
ALTFUN_RPC_URL=https://rpc.hyperliquid.xyz/evm
ALTFUN_POLL_INTERVAL_MS=15000
ALTFUN_LABEL=alt.fun
# {token} and {pair} are substituted with the new token and the pool address.
# alt.fun's coin page is the most useful destination; HyperSwap link works too
# (https://app.hyperswap.exchange/swap?outputCurrency={token}) — your choice.
ALTFUN_TRADING_URL_TEMPLATE=https://alt.fun/coin/{token}
# Defaults to "<dir-of-STATE_FILE_PATH>/altfun-state.json".
ALTFUN_STATE_FILE_PATH=
